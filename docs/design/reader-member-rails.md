# 正文旁的成员侧轨

2026-09-22：用户已试读并认可真实历史交互样稿，正式 Reader 接入及桌面验收完成。
2026-09-23：后续反馈要求稳定槽位、消除孤立卡片和未知虚线尾部；本批验收与发布状态见
[反馈执行账本](../specs/reader-feedback-2026-09/tasks.md)。

## 内容与形式

正文是主 Agent 的主线。右侧窄区域只放成员轨道，与正文共享纵向位置和滚动；左侧目录保留。
轨道按成员身份固定颜色与顺序，名称在对应轨道上保持可见。正文仍以请求、回复和关键协作内容
为阅读核心，模型与用量保持紧凑直显。

| 信息 | 呈现 |
| --- | --- |
| 创建成员 | 从正文发生位置指向该成员轨道的起点 |
| 成员间消息 | 从发送成员的空心节点，连接到接收成员的实心节点；箭头指向接收端 |
| 返回主 Agent | 从成员的发送位置指向正文实际收到消息的位置 |
| 派遣后主线继续 | 正文照常阅读，成员轨道从旁连续经过 |
| 消息与完整子历史 | 点击节点展开对应原文；原有完整子历史入口与关闭返回保留 |
| 多成员 | 附近最多四个稳定槽位；容量外卡片提供就地聚焦，正文不跳转；全体成员选择器另可定位其第一条记录 |

发送和送达必须由明确的同一消息 ID 配对；送达不能画成另一条回复。返回主 Agent 使用明确
的主会话身份，绘制位置为正文边缘，不为主 Agent 新增一列。颜色编码成员身份，不编码状态。
节点仍提供可聚焦链接、成员名与可访问方向说明，视觉上不重复占用一行方向文字。

成员按本文首次出现顺序分配六色，前六名不发生颜色碰撞；更多成员循环使用色板，始终配合
轨道名称识别。成员选择与滚动不改变同一本文中的颜色。当前窗口最多绘制 80 条关系；跨窗口
长连线按可见区裁剪，保留真实端点，裁剪边界不伪装成发送或送达节点。

纵向距离表达正文阅读顺序，不表达实际耗时。执行折叠时节点对齐所属可见执行入口，展开后
回到原记录；异步内容加载和子历史展开同样需要重新测量。轨道止于最后一次交互，不再用未知
虚线尾部贯穿后文；新增交互读入后才延续。终点表达历史的交互范围，完成/失败/退出状态仍取自记录。

卡片的尺寸、颜色和精简结构独立于当前选中的成员。单纯滚动只裁剪 overlay，保留成员继续
使用原横向槽位；成员间通信的两端一起聚焦。父/子历史各自测量，不借用父卡片的轨道样式。

只有收到记录而缺少单独发送记录时，已知发送成员到正文/接收成员的箭头仍可呈现，
实心节点落在实际接收位置；不增加一个虚构的发送时刻。

## 样稿与验证边界

`tmp/reader-side-rails/server.mjs` 只读代理现有本地会话，`rails.js` 和 `rails.css` 叠加呈现。
六条真实源记录：reviewer 创建、writer 创建、writer 发给 reviewer 的发送/送达、reviewer 发给
主会话的发送/送达。完整历史来自原服务，未生成新的付费模型样本。

预览提供轨道开关和原版入口；样稿修改不改变 `src/`、provider 文件或 viewer 元数据。
检查命令为 `node --test tmp/reader-side-rails/rails.test.mjs`，实际视觉与交互结果记录于样稿目录
`design-qa.md`。正式通用实现按[交付计划 R5](runtime-delivery-plan.md#r5正文旁的成员侧轨2026-09-22)
推进，并遵循仓库完整受影响面验证矩阵。

# Runtime Reader 验收记录

更新：2026-09-20。依据 [产品呈现规范](runtime-presentation-contract.md) 的 P1–P14。

当前阶段状态与下一动作统一记录在[分阶段交付计划](runtime-delivery-plan.md)；本文保存检查细节。

本轮产品验收以人的阅读为准：默认能看懂请求、回复和工作转折，逐层展开能读完整过程，
并能理解分工、团队往来、异步分离和结果返回。工程交付、真实记录覆盖与用户的视觉认可
分别记录。原始证据与诊断是技术详情，默认界面使用读者能理解的内容和动作命名。

2026-09-20 用户确认产品面向桌面。既有窄屏改进与下文验证记录保留；此后不再追加窄屏
实现或测试，也不把窄屏缺口计入完成门槛。`qa:e2e` 的搜索、来源恢复与 reduced-motion
检查继续在桌面运行，原窄屏触控尺寸检查移出默认验收。

这里区分自动化覆盖、真实记录验证和未完成的阅读体验验收。已完成的切片不等于整个产品目标达成。
私人会话原文和截图仅保存在本地 QA 目录，不进入仓库。

| 场景 | 已有证据 | 尚需完成 |
| --- | --- | --- |
| P1 会话库查找 | 可读标题与自定义标题测试；真实会话库搜索“架构”并筛选有子任务，进入阅读后通过返回入口保留查询、七 provider 范围、子任务筛选和两条结果 | 本操作链通过；其他筛选组合仍由回归测试覆盖 |
| P2 简单问答 | 默认正文与无协作区域的渲染测试；本地 Pi 一问一答 smoke 记录在 1280×720 首屏直接展示请求与回复 | 简单阅读布局通过；样本是安装验证记录，不代表复杂 Pi 工作 |
| P3 过程与消息阶段 | 消息阶段及 reasoning 归属测试；真实 Codex 双问答在正文、目录中可读，原始记录完整折叠；桌面及 390px、刷新、API 定位及搜索去重通过；task preview 使用同一投影并有回归 | 本次结构化问答通过；长问答续读和问答首条子任务预览由 fixture 覆盖，尚无对应真实长记录验收 |
| P4 并行任务 | 真实九任务会话；两审查任务相隔 8 秒派发，主任务继续实验；正文可沿同一任务逐步定位。侧栏同一十分钟窗口对齐七行二十条记录，相邻完成/接收/跟进可分别打开原文，桌面和 390px 明暗主题已检查 | 集中呈现与来源交互已有实页证据；用户阅读与视觉验收仍开放 |
| P5 重复跟进与返回 | 真实两次跟进、两次子侧完成、两次父侧接收按记录时间排列；六个来源均经键盘打开；21 条记录跨 11 页无漏重；390px 检查通过；P10 修复后重新验证子侧完成来源刷新与返回父侧接收 | 本场景的排序、来源和相关页面归属路径通过 |
| P6 多级子历史 | 真实三层历史展开、复制来源链接到新页、刷新、祖先路径及键盘返回；无重复 DOM ID；390px 三层正文均约 315px；普通返回精确保留位置与焦点，无旧位置的复制链接返回到可见入口 | 本次等宽阅读及祖先返回路径通过；整体视觉认可仍待用户使用反馈 |
| P7 上下文产物 | 真实 compact 141 条保留记录和 DSH 长结果续读；Codex stage1 记忆/摘要可完整读取。摘要旁现有按需后续整理分支：真实自动发现 1 次活动、7 条请求，全部 9 页与日志原文一致；原五字段 API、选定产物和来源文件哈希不变 | 保留诊断请求可读，完整生成历史仍不可达；stage1 producer、global job 外键、主会话使用及 dream 证据仍缺失。范围见[产物阅读](reader-memory-artifacts.md)及[后续整理记录](reader-memory-followups.md) |
| P8 继承背景 | 真实 Codex 四条/20 条及 DSH 39 条；DSH 五项 API 数据及来源文件哈希不变，实页默认折叠和 390px 展开通过。隔离 85 条 fixture 的未知来源、40→80→85 续读、失败重试、末页长字段和子面板 namespace 均经浏览器验证 | 超过 40 条和来源未知的边界已有契约及浏览器验证，但尚未找到对应真实背景样本 |
| P9 延迟加载与搜索 | 固定语料三类内容投影与第 1 阶段哈希一致；实际巨大历史后段搜索 4 处命中，定位展开、Escape 焦点恢复通过；真实 10,910 字符工具输出 4 页完整一致；真实任务目录 50→100 后可发现第 51 项。性能分段及本轮首载数值见下文 | 当前大页仍约 18 MB，初次解析成本保留；本轮没有将单次首载观测当作稳定性能提升 |
| P10 来源和历史归属 | 统一内联相关历史，root-owned URL 可复现子来源；实测父搜索 196 条、工具展开、关闭后准确位置与焦点保留；三层新页/刷新及 native/scalar 来源通过；单独阅读时所有页面操作一致切换 owner。9 月 19 日修复原生 hash 复用旧位置，真实主/子历史的跳转、Back/Forward 和窄屏刷新通过 | 来源定位缺陷已修复；初始化前未登记历史的工具栏可返回提示仍是近似判断，全产品视觉验收继续开放 |
| P11 视觉与可访问性 | 桌面 EN/ZH、明暗主题的 Library/Usage/Settings 与 Reader；子历史返回、侧栏键盘进入/Escape、可见焦点、长代码局部滚动、reduced-motion 通过；早期窄屏记录保留 | 用户整体视觉反馈开放；原始 Markdown 的标题跳级是已知语义问题，本轮保留原文结构，未声明全站 WCAG 认证 |
| P12 Provider 与缺失证据 | 七个已安装 provider 的 Reader/API 数据样本；不存在会话的 404；规范化诊断。OpenClaw 使用本地 smoke transcript | 样本通过不代表每个 provider 的所有演进特性均已覆盖；逐场景注明实际来源 |
| P13 团队成员合作 | 显式团队分支图与双向通信关系进入 Reader；原生格式隔离 fixture 经过真实 DSH adapter/server/browser，15 项分工、53 次往来、长正文末尾、无独立子文件及 11 成员目录续页可读；路由与客户端覆盖错误/修订重试 | 缺少原生两成员合作→结果汇入主线的真实记录；没有将合成 fixture 或七 provider API 通过当作原生 Teams 验收 |
| P14 后台程序与异步执行 | 真实 Codex 外层 exec/wait 成功与失败；独立终端 3 个程序/14 步，正常结束、轮询、Ctrl-C 请求后 exit 0；正文分离/返回、时间泳道、全日志、明暗主题及 Back 已检查 | 取消终态、尚未返回、多后台并行以明确 fixture 覆盖为主；未从停止请求推断已取消 |

## P 编号之外的明确剩余项

### 本轮增量验收：第 3B、4 与第 5 阶段（2026-09-20）

以下为本轮已经取得的结果；最终提交、全量回归和 Teams 实源状态由交付计划汇总。

实现提交为 `6a0a2cd`、`06071f7`、`167c882`，均已推送；代码版本的
[Quality 35487720410](https://github.com/Guanzhw/AgentSession/actions/runs/35487720410)
在 Node 22.15.0 / 26.5.0 上均成功。文档收尾不改变下述代码检查结果。

- 第 3B 阶段使用真实 Codex Desktop 0.142.0 会话
  `019efac9-94f3-7b50-98c7-94fcce90c301`。独立终端调用得到 3 个后台命令、14 个步骤。
  handle 80197 正常返回为 4 步；97155 为启动、分离、等待、仍在执行、Ctrl-C 请求、exit 0
  共 6 步。实际页面将请求停止和最终完成分开显示，没有推断已取消。
- 时间图、逐步原文入口与浏览器 Back 已在桌面检查；返回原位仍保持展开和全部 6 步。
  原文定位在粘性导航下方约 88px。长输出经浏览器“显示更多”读完 4 页，10,910 字符，
  SHA-256 `145352dd99bcfd5b43d228314bf91cfcf156cb63d6b2bbbd8fac43884a1078f1`
  与原始工具输出一致。源文件 SHA-256 为
  `270a1abecb1f3bf0bde69d394c638d2f5dd6f25527e016af51067e3570e3dd5c`。
  实际明暗截图为本地 `tmp/stage3b-process-{dark,light}.png`。
- 第 5 阶段检查 EN 3456 / ZH 3470 的 Library、Usage、Settings，1440×1000 明暗主题。
  搜索“调研本项目”→真实会话 `01a08ba7-0ca2-72b2-82a0-178d382c195f`→Back 保留
  查询和七 provider 筛选。六项图例为稳定 3×2 网格；筛选控件 38px，设置字段 39px；
  保存栏仍为 sticky；键盘焦点在两个主题均有 2px 强调轮廓与 3px 间隔。
  没有增加窄屏规则或测试。截图在本地 `tmp/stage5-qa/`，浏览器错误为空。
- 第 4 阶段使用隔离原生格式 fixture，经实际 DSH adapter/server/browser 检查，不执行模型。
  同一团队节点连接 Alice/Bob，通信边分别展开该方向的往来。Alice 分工从 12→15、
  往来从 50→53 续读；第 15 项长分工与第一条超过 12,000 字符的消息均读到末哨兵。
  最后一条只有排队记录，没有虚构送达；Bob 没有独立子文件时，已有往来仍可读。
  11 成员样例的目录由 8→13 项（成员与通信关系）续页，第 11 个成员可打开；搜索
  第 15 项分工末尾的唯一字符串仍命中 Alice。全页宽度保持 1440px。
  独立混合样例复核确认 Team 成员 run/任务去重后，普通 workflow 与 canonical 子历史入口保留。
  最终实页修复了 source 导航在打开子历史后覆盖父焦点的缺陷：打开 Alice 完整历史后 Back，
  恢复原 Team history 链接焦点、Alice 详情和单个根面板。图的明暗截图在
  `tmp/stage4-team-fixture/graph-release-{dark,light}.png`。
  中文图与成员详情的明暗实页补查已完成；局部 axe 检出并修复了新组件标题跳级，
  复查图和成员详情均为 0 违规。图的分页范围明确标注，完整目录保持可达。
- 统一构建在当前 Node 与最低 Node 22.15.0 上全套 1,008/1,008 通过。
  正式服务单独重启后，七 provider 的真实 Reader、预览、协议 API 通过，四个不存在路径返回 404。
  固定真实样本的 owned Reader、messages、inherited 三类投影与第 1 阶段基线逐字节哈希一致。
- 整站桌面 E2E 在最终导航修复后重跑通过，浏览器错误为空；随后 Team 局部标题和分页提示
  的调整又通过全量测试及实际 Team 页面检查。固定 129 文件语料复测：HTML route 单次
  2,364 ms，只读取根文件 499,432,851 bytes，HTML 为 16,425,792 bytes；新增阅读结构后
  HTML 不再与旧版逐字节相等，原始内容投影相等如上。持续增长的大型实页另测
  TTFB 3.04 秒、DOMContentLoaded 4.31 秒、DOM 约 17.98 MB，1440px 无全页横向溢出。
  后者是当前实页观测，不和固定输入混为性能对照。

### 2026-09-20 已关闭的实现差距与剩余实源边界

第 1 阶段已将具体要求与各次回复放入逐层阅读路径，上下文产物先呈现内容。
第 2 阶段已补齐超过 50 项的任务目录，并由局部关系图进入往来及完整子历史。
第 3B/4 阶段将后台程序与明确团队关系接入 Reader。实现不再以原始 Work 图作为主要入口。

剩余实源边界是 P13 原生团队合作、P14 未观测终态，以及 provider 没有保存的完整 memory/dream
生成历史。已保存内容可完整阅读；缺失内容与用户的视觉反馈分别跟踪，见[交付计划](runtime-delivery-plan.md)。

会话库现已按记录中的父子关系展示可展开家族。真实三层、子会话搜索路径、键盘展开、
独立阅读与返回、20/40 条子级分页和 60 个主条目恢复已验证；缺父/隐藏父、过滤组合与
实时数据库更新由 fixtures 覆盖。七 provider 元数据 API、桌面/390px、明暗主题和整站
E2E 通过。实现范围与具体证据见 [会话库分组](library-session-families.md)。

P4 已核对真实两子任务并行、主任务继续的记录，并验证同一任务的逐步导航及侧栏同期活动图。
见 [局部任务步骤](runtime-inline-task-steps.md) 和 [同期活动图](runtime-activity-map.md)。
用户对集中呈现和阅读体验的验收仍开放。
P7 已呈现 stage1 保存的完整内容、匹配作业和一条真实 Phase2 后续整理请求链，完整生成历史不可达。
用户视觉认可继续开放；这些边界不由 provider 样本通过或当前回归结果代替。

## 本轮真实缺陷

### P7 摘要旁的后续整理请求（2026-09-19）

来源历史 `01a0aa62-1091-7961-a883-c3e732fe548c` 的 V1 summary 通过
独立、按需的 artifact evidence 入口，自动找到生成 thread
`01a0adc6-d841-7b33-ac46-2a93664198da` / turn
`01a0adc6-d88d-7c12-b475-c4d205cd9d0e`。请求中没有预先指定生成 ID。
默认检查生成后首小时，访问 1,919 条日志元数据，读取 171,292 字节，
一页找到 1 次整理活动、1 条读取请求和 6 条修改请求。首次直接 provider
查询约 131 ms；一次后续 live API 查询约 60 ms，均为单次本机观测。

全部 7 条原文、9 个响应页与明确日志主键的原始请求文本逐页一致，其中
7,720 和 7,351 字符的请求各两页。原文只展示记录中的请求；当前文件与
stage1 summary 的绑定标为 derived，不据此推出执行成功。当前 state、
thread history 和相应日期的 rollout 中仍无生成历史，不创建失效链接。
完整来源会话 API 五字段、选定 stage1 行、summary 文件及复核日志哈希不变。

实页检查覆盖：

- 摘要正文展开时 evidence 请求为 0；单独展开后才发出 1 次查询。
- 键盘展开、鼠标/键盘续读到第二页；换成空范围后清除旧活动，并说明已检查范围。
  断网请求失败后可原地重试，保留原范围；随后能恢复指定范围及全部 7 条记录。
- 服务实际重启后，已展开的旧正文保持首段，后续请求返回 409 并给出正确来源
  历史刷新链接；刷新后重新检查可恢复。子 pane 的同类恢复由 route/client fixture 覆盖。
- 中英文、1280px 和 390px、明暗主题。重复修改行显示时间及展开箭头。
  长原文曾将整页撑至 54,747px，已通过两层 `minmax(0, 1fr)` 网格修复；
  新页加载后整页仍为 390px，原文仅在自己的区域横向滚动。
- 每页原文可获得键盘焦点，实测方向键令其 `scrollLeft` 增至 80px。
  新证据区域的局部 axe 检查（含展开长原文）中英/明暗均无违规；这不替代完整 P11。

`npm test` 916/916；Node 22.15.0 聚焦 43/43；七 provider Reader/API、
不存在 provider/会话的 404 和整站 E2E 通过。独立审查修正了保留行被清理后的
续页重验，以及同一多文件请求在字节预算处的继续位置。
原文、截图及检查输出保留在本机 `tmp/p7-followup-*`，不进入仓库。
产品定位和后续缺口保持本表定义；本次没有补造 dream、主请求使用或完整生成历史。

### DSH 继承背景与侧栏交互（P8 / P11）

DSH 的既有解析已将继承前缀排除在自身历史外，本次通过已有 `getInheritedContext()`
接口使它独立可读。真实样本的记录边界为 sequence 117，39 条背景完整匹配原始前缀，
自身消息仍为 2 条。生产 API 的 messages、tree、container、metrics 四项 SHA-256
修改前后完全一致，来源文件字节未变；桌面与 390px 展开和范围隔离通过。来源采用记录中的
canonical 父 ID，父文件缺失不抹去该引用。
超过 40 条及末页长字段有 v0–v3 fixture 覆盖，真实 39 条样本不充当跨页证据。见
[DSH 背景阅读决策](../../.agents/decisions/implemented/2026-09-17-dsh-inherited-context-reader.md)。

本轮将 `InheritedContextView.sourceSession` 放宽为可空：已记录的继承边界仍独立可读，
来源 ID 未记录时标明来源未知，不生成链接。晚加载页复用当前 Reader pane 的既有 DOM
命名空间。当前 `npm test` 883/883、聚焦检查 67/67 通过；真实 DSH 样本
`session-11f815ae-0057-4605-a67c-2f7d9e4b87dd` 的背景 API 返回 200、39 条。
重启后的五项 API 字段及来源文件 SHA-256 不变；39 条真实背景默认折叠，390px 展开无横向溢出。
本地有界调查尚未找到超过 40 条或缺来源 ID 的真实继承样本。隔离的 v3 fixture 在浏览器中
完成 40→80→85、断网失败后重试、末页长字段续读；内联子历史后页具有 pane namespace，
切换兄弟历史再打开仍保留 85 条，DOM ID 无重复。未知来源明确显示、无伪造链接。
英文深色桌面/窄屏、中文浅色 390px 及键盘激活通过；child pane 与未知来源 disclosure
两次局部 axe 检查均为零违规，不代表完整 P11 验收。聚焦 67 项也在 Node 22.15.0 通过；
七 provider 实际 Reader/runtime API、整站 E2E（无浏览器错误）、review 和 pre-push 均通过。见
[未知来源背景决策](../../.agents/decisions/implemented/2026-09-17-inherited-background-without-source.md)。

开启 reduced-motion 后，实页计算样式确认定位闪动、toast 滑入和 loading 旋转的动画为
`none`，定位位置仍有静态强调。侧栏打开后焦点移入关闭按钮，Tab 进入任务，Escape 返回
实际入口。页头、原生 summary、正文任务标记均经真实 Codex 页面验证；390px 原生入口的
相同路径也通过，页面无横向溢出。局部 Escape 停止冒泡，避免全局处理清掉恢复的焦点。
深色侧栏关键文字抽样对比度为 5.22–12.90，浅色为 5.12–14.88；这些抽样不代表全站通过。
完整键盘路径和对比度仍按 P11 单独验收。
此前该切片的 `npm test` 806/806、七 provider API 检查、整站 E2E（无浏览器错误）、review 和 Windows
binary smoke 均通过。HTTP 内容测试曾因 OS 分配的随机端口被 fetch 拒绝而失败，现以
Node HTTP 客户端验证同一实际路由，完整回归已通过。

### 往来记录的显示顺序（P5）

同一任务中，14:44 的跟进显示在 14:43 的父侧接收之前；子侧两次完成又被放在所有父侧接收之后。
来源时间本身可用，问题在 Reader 消费了按来源批次构造的数组顺序。修复属于共享 Reader 投影，
不改变 provider 原记录。修复后排序、完整集合状态摘要和续页完整性均已验收，
包括 120 条三页、已读前缀变化、正常后缀新增的回归。

### 子会话的页面操作归属（P10，已修复）

从目录打开没有就地分派锚点的相关子会话，会替换阅读正文并更新 canonical URL。
真实浏览器中，页面主标题、MD/JSON 导出链接，以及恢复、重命名、复制 ID、删除按钮仍指向父会话。
刷新同一 URL 后才全部成为子会话。验证只读取链接和按钮属性，没有执行修改或恢复操作。

这一结果推翻了“页头始终保留最初会话 owner 即可”的假设。需要让页面操作 owner 与当前 URL 的
含义一致，同时保留就地子历史与父阅读位置。不能通过更改验收文字把这个冲突视为通过。

实际修复统一了目录和协作入口：相关子历史保持就地打开，pathname、标题和全局操作共同属于根页面；
`readerSource` / `readerAncestor` 复现所选子来源。没有分派位置时明确标注，单独阅读才完整切换 owner。
真实父会话的 SQLite 查询、196 条结果和已展开工具保留，关闭后恢复到启动位置与控件。
三层来源复制到新页、刷新及逐级关闭通过；另一真实会话的子完成事件刷新后，Back 回到父侧接收记录。

首次并发 API 检查发生一次 `ECONNRESET`；服务进程仍在线且错误日志为空，重试完整通过。
七个 provider 的样本均无协议校验错误；OpenClaw 和 Pi 为本地安装 smoke 记录。
全套测试、整站 E2E、独立审查和 Windows binary 检查通过，数量及执行记录见
[Reader 检查点](runtime-reader-slice.md)。浏览器暴露的缺失客户端翻译已补齐并增加回归。

### 多级窄屏与结构化问答（P6 / P3，已修复）

真实三层历史在 390px 下原宽度约为 315 / 301 / 270px，现统一为约 315px。
祖先路径保留在正文流内；返回恢复原入口的焦点和位置。复制链接没有旧阅读位置时，
返回会揭示可见入口。桌面原有目录 resize handle 导致的 5px 横向溢出也已消除。

Codex 已记录的问答回复由 provider 转为中立展示字段；正文、目录、任务摘录和搜索共享
可读问答，原文与 canonical ID 保留。真实双问答的原文展开后，搜索仍只命中一次。
758 项测试、七 provider API、整站 E2E 和 Windows binary smoke 均通过。

### 超长活动会话加载（P9，开放）

超长活动 Codex 会话的完整 API 请求等待超过四分钟，同期页面导航超时；进程仍在线、
CPU 持续增长且错误日志为空。该请求随后被部署重启中断，因此最终连接重置不是自发崩溃证据。
较小真实会话已通过上述验收，但不能替代这个性能场景。

独立进程的分段诊断已区分两个问题：冷元数据索引读取 535 个文件、约 2.24 GB，耗时
8.0 秒；随后完整 scan 耗时 209.3 秒、读取约 59.66 GB，其中约 56.57 GB 是同一个
432 MB 父会话的重复读取。已有任务边界的子会话不需要父正文来判定归属，重复读取
来自消费路径，而非源数据规模本身。旧 tree/container 缓存还把构建时间计入一秒有效期，
导致慢构建结果立即失效。

修复后，另一次 535 条真实会话 scan 耗时 15.1 秒、读取约 4.16 GB；同一个父文件的
累计读取约 2.16 GB。两次观测之间活动记录继续增长，因此不是完全相同输入的基准。
确定性回归验证有任务边界的子会话不加载父正文，旧 fork 仍读取父证据，并保留继承
背景、来源 ID、token 归属以及 v2/v3 对继承原始事件的过滤。

稳定真实九子任务会话的 tree、container、metrics 序列化 SHA-256 在修复前后分别完全一致。
超大会话的独立 tree 构建耗时 8.7 秒，紧接的 container 命中缓存、未读取文件；metrics
耗时 5.5 秒。生产服务重启后的 Codex 索引为 535 条 / 28.0 秒。七 provider 的常规
Reader/page/preview/API、四项 404、整站 E2E、真实子历史展开和返回、763 项测试及
Windows binary smoke 通过。

上一检查点的超大会话完整 API 未通过：`JSON.stringify` 抛出 `Invalid string length`，
随后错误响应尝试修改已经发送的响应头，连接被关闭。进程仍在线，后续常规检查通过。
这条接口同时返回 messages、tree 和 container；问题属于完整响应的字符串构造。

随后改为保持原五字段合同的有界流式序列化。稳定真实会话的 compact API 和 pretty JSON
导出均与修改前逐字节一致。原失败的活动根会话现返回约 722 MB API 和 720 MB 导出，
独立流式解析器分别在 29.5 / 28.5 秒内读到完整 JSON 结尾，后续健康检查为 7–10 ms。
重启后的服务日志无序列化或重复响应头错误。两个大响应之间活动数据继续增长，且导出
消息投影不同，不用其相互字节差异推断内容丢失。见
[流式决策](../../.agents/decisions/implemented/2026-09-17-large-session-json-streaming.md)。

另一独立生产 HTML 路由诊断返回 200，但生成完整页面仍需 19.7 秒，HTML 约 35.2 MB。
这项首屏加载成本与 scan 重读分开验收；应继续测量初始正文、完整续读、搜索定位和
协作来源的加载路径，不能把后端扫描加速等同于长历史阅读体验通过。

折叠工具和推理正文现经既有 `/content` 接口按需加载，全文搜索仍读取完整来源。
真实 27,929 字符工具输出在未加载时可命中后页并加载到 offset 6,000；浏览器阻断内容请求
后可见局部失败，解除阻断并重试恢复两字段且移除错误提示。来源链接新导航、刷新与 390px
展开检查通过。七 provider 检查及整站 E2E 通过。

最终超长 HTML 为 23,745,775 字节、225,445 元素，原快照为 35,579,034 字节、232,296 元素。
原有 1,557 个正文块哈希一致，2,034 个 turn 和 10,115 个 part 锚点无缺失、相对顺序一致。
活动会话在两次采样间新增内容，不能当作固定输入的速度基准。最终 HTTP 页面生成约 13.6 秒；
超长实页能完成浏览器打开，但仍超出导航工具一次默认等待窗口。该性能场景继续开放。

下一切片将普通工具过程改为每段最多 20 项，保留初始正文、协作入口、异常数量和全部来源锚点。
稳定九任务会话的 HTML 从 1,647,042 降至 1,368,844 字节，元素从 13,004 降至 9,232；
48 个正文块逐字节相同，408 个消息/part 锚点及顺序完全相同。活动长会话为 14,252,388 字节、
114,421 元素，上一快照的 1,565 个正文块哈希和 10,888 个消息/part 锚点无缺失，锚点相对顺序保持。
其间新增 11 个正文块和 133 个锚点，不能用这个活动样本作固定输入速度基准。

真实浏览器中，未加载工具的 canonical 链接会先取得所属片段再定位；全文搜索仍能命中并展开
27,929 字符输出的后页。阻断过程请求后显示局部重试，恢复网络即可读取。390px 子历史晚到片段
保持 scoped ID 和约 315px 工具宽度，关闭后父搜索、展开状态和焦点保留。真实长页的 22 项过程
只自动加载前 20 项，点击续读后余下两项完整出现，来源 ID 无重复、焦点位于新工具摘要，浏览器无错误。
独立审查发现的异步抢焦点与活动记录追加后旧片段 404 已修复。

795 项测试、整站 E2E、七 provider 常规 API 和 Windows binary build/smoke 通过；实际过程片段
接口另在 Codex、DSH、OpenClaw 记录上通过。OpenCode E2E 样本没有可延迟的普通工具，不能把它
算作该功能的实页证据。最终长页 HTTP 首字节约 14.24 秒，首载性能仍开放。分段诊断显示
provider 记录解析与 v2/v3 重复准备仍需单独处理。机制见
[工具过程加载决策](../../.agents/decisions/implemented/2026-09-17-deferred-reader-execution.md)。

本次进一步将 Reader 所需的 v2/v3 作为同一 provider 输入的配对快照准备。稳定真实会话的
v2、v3、ownedReader、messages、metrics 五项 SHA-256 前后完全一致，两版协议验证无错误
或警告。活动巨型会话的协议文件读取从两次调用合计 2,004,578,613 字节降至一次调用
775,797,365 字节（约 2.00 → 0.78 GB）；该配对调用耗时 5.32 秒，完整 HTML route
前后观测为 20.40 → 14.27 秒。数据在两次测量之间持续增长，这不是固定输入速度基准；
正文准备与初始页面成本仍存在，P9 保持开放。见
[配对协议快照决策](../../.agents/decisions/implemented/2026-09-17-atomic-reader-protocol-snapshots.md)。

#### Reader 请求内快照：固定输入对照

将巨型根及其直接子会话、稳定九任务根及其直接子会话复制为只读测试输入，共 129 个文件、
1,329,667,016 字节；只复制捕获时已有的完整 JSONL 行，原 provider 文件不变。准备耗时
1.39 秒。根文件 499,432,851 字节，SHA-256 为
`c7fa9e06484d52b1f205c3c621919a279df67f186f4c86ecf11ddea47af05ebd`。
该私人语料与探针输出只保存在本地 `tmp/runtime-performance/`。

Node 26.5.1 下，每次测量启动独立进程、先完成元数据索引，再调用实际 HTML route。
旧实现三次耗时 9.404 / 9.758 / 11.336 秒；请求内快照为 7.678 / 7.439 / 7.471 秒，
中位数下降约 23%。这里不含索引、网络传输和浏览器布局时间，也不代表全站并发基准。
有完整 I/O 计数的两次旧实现均读取 1,798,104,292 字节，其中根读取 998,865,702 字节；
三次新实现均读取 1,298,671,441 字节，其中根只读取 499,432,851 字节。其余读取主要是
协议的直接子记录准备；未据运行间浮动的 RSS 声称内存改善。

仅替换 HTML 中 `message-time` 的相对时间文本后，整个前后页面逐字节一致，SHA-256 为
`c9daea588b8dc7e61d8281334013dd875cbd331f5a718811ecde6ab6e55d8e85`。
稳定九任务样本的 v2、v3、owned Reader、messages、inherited 五项独立 SHA-256
亦分别一致。快照保留正文和来源，而不是通过删减历史来缩短加载。

861 项测试、独立审查、七 provider 实际 Reader/page/preview/runtime API 及整站 E2E
通过，无浏览器错误。六项新增快照回归也在最低 Node 22.15.0 下通过。重启后的 Codex
索引为 544 条 / 23.3 秒，服务错误日志为空。实际巨型页约 15.31 MB，浏览器观测
TTFB 10.78 秒、响应读取结束 11.96 秒、DOMContentLoaded 24.35 秒；该次检查与
E2E 共享服务且输入继续增长，属于实页观测而不是上述固定基准。工具字段加载完成，
136 个 part 的子历史就地展开并可返回，父工具展开状态保留。机制见
[请求内快照决策](../../.agents/decisions/implemented/2026-09-17-request-local-reader-snapshot.md)。

#### 2026-09-19：去除重复解析与重复查找

本轮只调整既有算法，不改变阅读内容或协议合同：

- Codex 子会话继续读取并按父记录判定归属；协议直接使用索引已有的身份与时间元数据，
  不再为未消费的消息数量、token 与继承计数重新解析子正文。
- v3 协作调用结果按 call ID 建立一次索引，保留原先“第一条匹配输出”的语义，
  替代每次调用遍历全部记录。
- 浏览器为当前 Reader pane 一次建立锚点映射，替代每个 ToC 链接重新扫描整个 pane；
  保留子历史 namespace、canonical 锚点及原有失效更新机制。

**后端固定输入。** 沿用上述 129 个文件、1,329,667,016 字节语料，在 Node 26.5.1
的独立进程中完成索引后测量实际 HTML route。隔离基线将 `48129bc` 的 Codex adapter
和 protocol 两个模块转译至测试构建，其余输入和构建保持相同。基线三次为
7.880 / 6.830 / 6.784 秒，当前实现为 6.034 / 6.603 / 5.328 秒；中位数
6.83 → 6.03 秒，下降约 12%。六次读取均为 1,298,671,441 字节，HTML 均为
15,272,332 字节；收益来自重复计算减少，未减少本轮文件读取量。

仅统一 `message-time` 的相对时间文本后，六份完整 HTML 逐字节一致，SHA-256 为
`45622921c802e2b29d30e35ba4f943d96d36e1b4564b8e31827360e858246535`。

**浏览器固定输入。** 使用同一份 15.27 MB HTML，只替换前后客户端 bundle；两版 QA
bundle 均在 `initSessionWorkbench` 调用外计时。单位为毫秒：

| 观测 | 基线三次 | 当前三次 | 中位数 |
| --- | --- | --- | --- |
| Workbench 初始化 | 3901 / 3580 / 5113.2 | 146.9 / 191.3 / 190 | 3901 → 190 |
| DOMContentLoaded | 5188.6 / 5037.3 / 6313.7 | 1684.9 / 2023.1 / 1618.8 | 5188.6 → 1684.9 |

两版均为 123,517 个元素、14,580 个锚点、755 个页内 ToC 条目。该浏览器基准不含
provider 准备时间，不能与独立后端基准相加作为一次实际首载。原始计数及一致性报告位于
本地 `tmp/runtime-performance/p9-parity-report-20260919.json`。

本轮统一构建后 `npm test` 887/887 通过；新增回归完整比较 task-envelope、legacy
copied-prefix 与 fork 的 finalized v2/v3，以及子会话自身消息和 token 归属。
Node 22.15.0 的协议、子归属和 Reader 聚焦回归 40/40 通过。七个 provider 的真实
Reader/API、协议验证和不存在会话的 404 检查通过；整站 `npm run qa:e2e` 通过，
浏览器错误为空。九任务真实样本的 v2/v3、owned、messages、inherited 五项完整投影
在前后实现中哈希一致（仅协议 revision 不参与比较）。

生产服务上的持续增长大页观测为 TTFB 6.83 秒、DOMContentLoaded 8.49 秒，响应
15,690,367 字节、11,740 个 part、882 个 ToC 条目。它与固定基准输入不同，单独报告。
新标签页打开末段来源链接可准确定位；九任务样本在 390px 下从 ToC 展开子历史、
定位子正文、关闭并返回入口均通过，焦点和 root-owned URL 正确，无重复 DOM ID 或
全页横向溢出。截图保存在本地 `tmp/runtime-performance/p9-child-*-20260919.png`。

**当时发现的 P10 缺口（下节已修复）。** 在已有标签页对同一路径导航到新的原生 hash，来源收到焦点后，
滚动位置仍被旧历史条目恢复为页首。`session-reader.js` 的 `popstate` 将缺失
`readerEntry` 的原生记录复用为条目 0，覆盖了新 hash 的定位。该代码来自 `0668b6fd`，
本轮未修改；新页深链接及受管子历史返回不受此复现影响。后续需分别处理原生片段导航
和受管历史恢复，并覆盖两者的 Back/Forward 行为。

P9 的大文件初次解析成本、完整 P11 以及用户阅读与视觉验收仍开放。

### P10 原生来源跳转与独立阅读位置（2026-09-19）

同页原生片段导航创建新的浏览器记录。Reader 现在直接登记该记录的独立身份和空阅读位置，
首次按来源定位；后退和前进恢复各条记录后来读到的位置与焦点。登记采用 `replaceState`，
不额外创建浏览器历史。已知身份保持稳定，首次访问更早的未登记条目后，新导航也不会覆盖它。
子历史在返回时恢复原有正文、展开状态和焦点，延迟来源响应不能覆盖更新的导航。

验收证据：

- 四个新回归在修复前全部失败，修复后导航测试 59/59；`npm test` 891/891 通过。
  Node 22.15.0 的导航、位置与 workbench 测试 74/74 通过。
- 真实 Codex 九任务会话：修复前从 800px 原位打开末段来源，焦点移动但最终仍回到 800px。
  修复后首次来源可见；两个来源分别读到 20,929px 和 12,583px 后，连续 Back/Forward
  精确恢复两个位置和对应焦点，返回初始页恢复 800px。
- 390px 下来源跳转与同宽刷新均保持目标可见（页顶偏移约 72px）、历史身份不变；
  已展开子历史再跳主来源后，Back 恢复同一个子 pane、6,998px 阅读位置和原焦点。
  无重复 DOM ID、全页横向溢出或浏览器错误。
- `npm run qa:e2e` 通过。新增真实 OpenCode 的原生 fragment → Back → Forward → Back
  链路检查来源可见性、焦点及父位置；既有搜索、子历史和全站检查继续通过。
- 独立审查、`npm run review` 及 `git diff --check` 通过。

本地日志为 `tmp/p10-test-20260919.log`、`tmp/p10-e2e-20260919.log`；
窄屏来源截图为 `tmp/p10-native-source-390-20260919.png`。Provider 数据及接口未改变。
初始页面登记前的旧原生记录已覆盖身份与往返回归，但工具栏仍用既有编号近似判断能否返回，
不能据此推断浏览器栈的起点；该提示的完整可用性留待集中导航验收。

### 第 1 阶段：可读会话与逐次协作正文（2026-09-20）

交付范围是[分阶段计划](runtime-delivery-plan.md)的 1.1–1.5。默认保留用户请求、回复和
关键转折；任务侧栏先给目录及历次往来，原始记录与运行诊断留在技术详情。展开某次往来
才加载该次正文，长内容使用与原文哈希绑定的续读。compact 和 memory 均先给可读结果。

**实际阅读路径：**

1. 打开九任务会话 `/codex/session/01a08b93-9674-7b90-acb3-6fde919efebf`，先读主线。
2. 打开 Tasks & collaboration，选择 `/root/first_unit`，展开两次 Task finished 和
   Result received。各自对应源文件中的同一次返回，而非“最近回复”的替代。
3. 展开 Additional instructions；本样本原文是加密载荷，页面说明无法读取，不显示密文。
4. 进入完整子历史，关闭后回到原任务与展开状态，焦点回到原入口。
5. 打开 `/codex/session/01a0aa62-1091-7961-a883-c3e732fe548c` 的保存内容；或在
   `/codex/session/01a0576a-98e2-7c31-a265-6d98d5fbff12` 的第一个 compact 展开保留上下文。

**检查结果：**

- `npm test` 948/948；Node 22.15.0 的 provider/正文/compact 聚焦回归 36/36。
  独立审查发现并修复了 provider 全局版本造成的误报 409、同 ordinal 的 turn 身份问题及
  UTF-16 分页切断 emoji；最后两项补回归后全套仍通过。
- 九任务真实记录的分派、两次追加、两次子完成、两次主线接收共 7 项与原文逐项核对。
  可读结果分别为 1370/1392 字，正文 SHA-256、长度及 HTML 续页一致；3 项加密要求明确不可读。
  与当前活跃会话并存时，稳定旧来源正常返回 200。
- 实页分别打开追加、完成、接收；完整子历史有 184 个 part，关闭后父页恢复一个 pane、
  原选中任务、3 个已展开正文与原入口焦点，重复 DOM ID 为 0。活动概览在自身展开前请求为 0。
- Pi 简单问答直接呈现请求与回复，没有空协作框。memory/summary 正文、独立后续整理、
  continuation 与键盘展开通过。compact 的 7 个条目级技术详情、来源与加密说明默认折叠；
  有保留上下文时不把空摘要提示放在正文前。主线程查看截图后纠正了首轮 compact 层级检查遗漏。
- 桌面 EN 浅色检查简单问答、memory 和 compact；EN 深色、ZH 浅色检查 memory；ZH 深色
  检查 memory 和 compact。整站桌面 E2E 通过，搜索 Escape、子历史、原生来源 Back/Forward、
  技术详情、reduced-motion、导出及设置等路径覆盖，浏览器错误为空。未做窄屏验证。
- 七个 provider 均完成真实 Reader、预览与协议 API 检查，协议 validation.ok 为 true；
  4 个不存在 provider/session 路径返回 404。这不代表七个 provider 都记录所有运行时特性。
- 固定真实语料的配套优化见[索引事实决策](../../.agents/decisions/implemented/2026-09-20-indexed-codex-child-evidence.md)：
  HTML route 中位数 6812 → 2688 ms，完整 HTML 与五类投影一致。持续增长实页另测约
  16.72 MB、TTFB 2.94 秒、DOMContentLoaded 5.35 秒；该观测不是固定输入基准。
  完整历史搜索找到末段工具，展开输入续读，Escape 保留结果焦点，无整页横向溢出。

本地日志：`tmp/stage1-final-{test,node22,coordination,api,e2e,review}-20260920.log`。
截图：`tmp/stage1-first-return-final-20260920.png`、
`tmp/stage1-memory-20260920/compact-final-{en-light,zh-dark}.png`，以及同目录 memory/Pi 截图。
它们与用户提供的 Work/Conversation 旧图对照；完整内容保留，原始技术字段退至按需展开层。
提交及远端 CI 记录在交付计划更新。当前任务名称、50 项目录和全站视觉统一仍属于后续阶段，
用户对整体视觉的认可另行记录，不能由测试通过代替。

### 第 2 阶段：完整任务目录与局部关系图（2026-09-20）

交付范围为计划 2.1–2.4。图解释分派和返回，往来时间线解释顺序，完整正文继续展开阅读，
搜索目录用于发现全部任务。图中的参与者使用记录的 actor ID；普通 mailbox 消息保持消息含义。
同一子历史的多次运行保留一个任务入口，初载每组一个运行，后续每页至多 50 个运行。
图的关系汇总独立于正文分页，单个视觉窗口至多四条参与者与任务的联系。

**实际阅读路径：**

1. 九任务会话打开“任务与协作”，在图中选择 `first_unit`，下方读取 21 条往来中的
   两次完成与两次返回；图的第二组可选择 `state_estimation`。
2. 大会话 `/codex/session/01a0576a-98e2-7c31-a265-6d98d5fbff12` 打开任务目录并续读。
   第 51 个规范任务是 `ui_p5_hash_worker`，不再受最初 50 项限制。
3. 打开该任务的完整子历史；浏览器后退恢复同一任务、已加载的目录页、侧栏及原入口焦点。
4. 搜索 `human_coordination_content` 可直接找到后续任务，选中后仍可读取往来与子历史。

**已取得的检查结果：**

- 最终 `npm test` 972/972；Node 22.15.0 的目录、图、provider、导航及客户端回归 133/133。
  最终构建的整站桌面 `npm run qa:e2e` 通过，浏览器错误为空。
- 真实大会话的目录从 143 项增长到最终接口检查的 146 项；最终三页为 50/50/46，规范 key
  无重复。第 51 项详情与完整 child Reader 返回 200。查询超出 50 的页大小、同时给 key/lane
  返回 400，缺失任务返回 404。这些是不同时间点的活跃实源检查，不是固定输入性能基准。
- 第 51 项实页子历史有 40 个 part；Back 后恢复一个父 pane、原任务、两页目录、展开侧栏及
  `reader-child-history-link` 焦点。重复 DOM ID 为 0；活动概览未展开时请求为 0。
- 在浏览器中人为延迟两次真实搜索请求并逆序放行，新查询 `human_coordination_content`
  的结果未被先前 `provider_normalization` 的晚到结果覆盖。
- 对后续任务的一次请求注入 404：目录关闭时加载失败及重试按钮仍可见；点击重试后真实
  任务成功打开。模拟只发生在测试浏览器网络层，没有改动 provider 文件。
- 九任务实页检查 EN 浅色、ZH 深色，图节点及边选择、跨组、关闭与 Escape 均通过；
  同一任务的多次往来和完整历史保持可读。主线程查看了实际截图，未以截图存在代替检查。
- 七个 provider 的真实 Reader、完整任务目录及可用的首个任务详情通过；四个缺失
  provider/session 路径返回 404。未产生关系的普通会话没有空图。
- 独立审查提出的子 pane 命名空间、不可见失败提示、普通消息误标结果、分组突破初载
  上限、快速查询覆盖、首运行插入后的游标失效及跨运行关系遗漏均已修正并补回归。

本地日志：`tmp/stage2-release-{test,node22,directory,api,e2e}-20260920.log`；
实页截图：`tmp/stage2-release-zh-graph-20260920.png`、
`tmp/stage2-release-task51-20260920.png`、`tmp/stage2-task51-final-20260920.png`，
以及 `tmp/stage2-graph-qa-20260920/`。

另在明确标注为合成数据的独立 loopback fixture 中，使用实际 Reader、路由、脚本和样式验证：
同一子会话的 120 次运行通过 1 + 50 + 50 + 19 连续加载完毕，120 个原始运行 ID 均唯一，
首末运行各出现一次，加载完成后没有多余续读按钮。九个同名但 ID 不同的参与者保留三个图形
窗口；翻到第三组仍指向同一任务，节点与参与者无重叠。三次任务请求均为 200，无 JS 错误；
唯一 404 是该 fixture 未提供的 favicon。截图 `tmp/stage2-multirun-qa/graph-page-3.png`、
`tmp/stage2-multirun-qa/runs-120.png` 经主线程查看；测试浏览器和独立服务已关闭。
该合成检查补充真实第 51 项与九任务页面，不代替实源验收。桌面验收范围保持不变；
Teams 与后台生命周期属于后续阶段。

### 第 3A 阶段：异步工具的分离、等待与返回（2026-09-20）

本切片使用已记录的外层异步工具身份，将正文两处关键节点和局部时间图连接起来。
普通命令与全部输出仍在完整历史中；图中的每一步可以定位原文，密集等待按需展开。
共享呈现消费 `execution` 观察，Codex 负责实际 `exec` / `wait` 语法和 cell handle 关联。
代码提交 `45e7ad5d84e3ed40b66c520025739db16d420b1a` 已推送，远端 SHA 一致；
[CI 35464094794](https://github.com/Guanzhw/AgentSession/actions/runs/35464094794)
的 Node 22.15.0 / 26.5.0 均成功。

**实际阅读路径：**

1. 打开 `/codex/session/019feef9-6769-79d0-b98b-778a7155b948`，定位
   `execution-event-tool-execution-call_R5k7snSOmnVU7iB8r5XgWfK0-yielded-call_R5k7snSOmnVU7iB8r5XgWfK0`。
2. 展开“查看执行经过”：主线和后台使用相同时间轴，开始于 04:00:39 UTC，04:00:50 分离，
   04:01:14 收到失败结果。分离连线位于 33.0906%，与 yielded 对齐；不是 2% 的启动点。
3. 展开全部 6 步，逐项读取启动、两次转入后台、两次等待和失败对应的完整命令/输出。
4. 从原文 Back 回到已展开的执行图；点击“前往返回结果”，再直接“回到分离位置”。
   展开状态保留，两处目标均获得焦点且位于 sticky header 下方，无需补滚动。
5. 成功返回另核对大会话 `01a0576a-98e2-7c31-a265-6d98d5fbff12` 的
   `call_ViwvBapSXyHpER6PlQKIT6Gm`（cell 65），原始记录 6804–6810 保存完整绑定。

**已取得的检查结果：**

- 最终构建后 `node --test test/*.test.mjs` 990/990；最低 Node 22.15.0 聚焦回归 107/107。
  此前 `npm test` 989/989；之后增加了跨时间窗端点回归。最终测试日志为
  `tmp/stage3-release-scoped-test-20260920.log`，最低版本日志为 `tmp/stage3-node22-20260920.log`。
- 独立源代码审查提出的 public event 丢字段、共享来源重复索引、第一输出块识别、
  分离时间坐标及跨窗口端点错位已修正；不为未观察到的进程/取消语法添加分支。
- 桌面 1440×1000 的真实 ZH 浅色/深色页面检查、全部 6 步加载、原文与 Back 均通过。
  时间图 client/scroll 宽度均为 736px；无整页横向溢出，页面错误为 0。
  实页发现并修复跨节点跳转被顶栏挡住的问题，复验两个目标 top 分别为 87.72/87.59px。
- 合成回归覆盖并行调用、先启动后分离、跨窗端点、无返回、缺失/倒序时间及停止请求；
  122 步按 50/50/22 续读，允许源文件尾部增长，已读前缀变化返回 stale_page。
  浏览器模块回归验证单次加载、续页、原位重试及过期后重读，不修改已挂载的原文。
- 七个已安装 provider 的真实 Reader/page、预览和 runtime summary 均返回 200，协议校验
  完整合法；异步失败样本返回 6 步，成功的 cell 65 返回 4 步。非法 cursor 为 400，缺失
  execution/provider/session 为 404。七 provider 入口通过不代表它们都保存异步特性。
- `npm run pre-push` 的治理与类型检查通过。最终整站桌面 `npm run qa:e2e` 通过，浏览器
  错误为空，日志 `tmp/stage3-release-e2e-20260920.log`。此前一次用量导航等待超时，随后
  发现本地服务退出；恢复服务后完整重跑通过。服务日志没有额外异常输出。

截图位于本地 `tmp/stage3-browser-qa/`，其中
`zh-dark-crosslink-return-fixed.png` 已由主线程查看并在会话中展示；
`zh-light-call-r5k7-return.png` 为浅色检查。未进行窄屏验证。

**仍开放：** 本切片只表示外层异步调用，不表示其内部终端程序。所检查样本中的嵌套程序
缺少独立调用与返回的安全绑定；取消结果也未在真实样本出现。并行和长等待分页当前有
fixture 覆盖，不能据此宣称真实 Teams 或完整第 3 阶段验收通过。后续动作见分阶段计划。
